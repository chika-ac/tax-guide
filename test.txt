Function SplitCSV(s As String) As String()
    Dim res() As String
    res = Split(s, ",")
    Dim i As Integer
    For i = 0 To UBound(res)
        res(i) = Replace(res(i), Chr(34), "")
    Next i
    SplitCSV = res
End Function
