' 申請番号（12桁）
    re.Pattern = "(^|[^\d])\d{12}([^\d]|$)"
    result = re.Replace(result, "$1" & maskChar & "$2")

    ' 職員番号（7〜8桁）
    re.Pattern = "(^|[^\d])\d{7,8}([^\d]|$)"
    result = re.Replace(result, "$1" & maskChar & "$2")
