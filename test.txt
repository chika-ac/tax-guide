Function SecsToHMS(secs As Long) As String
    Dim h As Long, m As Long, s As Long
    h = secs \ 3600
    m = (secs Mod 3600) \ 60
    s = secs Mod 60
    SecsToHMS = Format(h, "0") & ":" & Format(m, "00") & ":" & Format(s, "00")
End Function
