' コメントテキストから数値部分だけ抽出
Dim commentText As String
commentText = Target.Comment.Text
' 改行で分割して最後の行（数値部分）を取得
Dim lines() As String
lines = Split(commentText, Chr(10))
originalColor = CLng(Trim(lines(UBound(lines))))
