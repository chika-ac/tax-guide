' 値のみ貼り付け
destWs.Cells(3, 1).Resize(copyRange.Rows.Count, copyRange.Columns.Count).Value = copyRange.Value

' 貼り付け後に全列の書式を標準に強制リセット
Dim resetRange As Range
Set resetRange = destWs.Cells(3, 1).Resize(copyRange.Rows.Count, copyRange.Columns.Count)
resetRange.NumberFormat = "General"
