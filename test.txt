If cntInb8 > 0 Then
    ws.Cells(outRow, 3).Value = Format(sumInbSec8 / cntInb8, "0.0")
Else
    ws.Cells(outRow, 3).Value = "-"
End If
If cntOut8 > 0 Then
    ws.Cells(outRow, 4).Value = Format(sumOutSec8 / cntOut8, "0.0")
Else
    ws.Cells(outRow, 4).Value = "-"
End If
